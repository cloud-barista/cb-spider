RESTSERVER=localhost

#CLOUD_NAMES=( cloud-1 cloud-10 )


num=0
while :
do
	#if [ $num -eq 100 ] ; then
	if [ $num -eq 5 ] ; then
                break
        else
		CLD_NUM=`expr $num + 1`
		CLOUD_NAMES[$num]=cloud-$CLD_NUM

	fi

	num=`expr $num + 1`
done


